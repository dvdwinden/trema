{{!< default}}

<main class="site-main">
    <div class="container">
        
        {{! Latest post }}
        {{#get "posts" limit="1" include="tags"}}
            {{#foreach posts}}
                <section class="latest-post-featured">
                    {{#if feature_image}}
                        <div class="latest-post-image">
                            <a href="{{url}}">
                                <img src="{{img_url feature_image size="l"}}" alt="{{title}}" />
                            </a>
                        </div>
                    {{/if}}
                    
                    <div class="latest-post-content">
                        <h1 class="latest-post-title">
                            <a href="{{url}}">{{title}}</a>
                        </h1>
                        {{#if primary_tag}}
                            <h2 class="latest-post-author">by {{primary_tag.name}}</h4>
                        {{/if}}
                        {{#if custom_excerpt}}
                            <p class="latest-post-excerpt">{{custom_excerpt}}</p>
                        {{else if excerpt}}
                            <p class="latest-post-excerpt">{{excerpt}}</p>
                        {{/if}}
                    </div>
                </section>
            {{/foreach}}
        {{/get}}
        
        {{! Latest in fiction}}
        <section class="section-fiction">
            <div class="section-header">
                <h2 class="section-heading">Latest in Fiction</h2>
                <a href="/tag/fiction/" class="section-see-all">See All</a>
            </div>
            
            {{#get "posts" filter="tag:fiction" limit="4" include="tags"}}
                <div class="posts-grid">
                    {{#foreach posts}}
                        <article class="post-item">
                            {{#if feature_image}}
                                <div class="post-image">
                                    <a href="{{url}}">
                                        <img src="{{img_url feature_image size="m"}}" alt="{{title}}" />
                                    </a>
                                </div>
                            {{/if}}
                            
                            <div class="post-content">
                                <h3 class="post-title">
                                    <a href="{{url}}">{{title}}</a>
                                </h3>
                                {{#if primary_tag}}
                                    <h4 class="article-author">by {{primary_tag.name}}</h4>
                                {{/if}}
                                {{#if excerpt}}
                                    <p class="post-excerpt">{{excerpt}}</p>
                                {{/if}}
                            </div>
                        </article>
                    {{/foreach}}
                </div>
            {{/get}}
        </section>
        
        {{! Newsletter signup}}
        <section class="newsletter-signup">
            <div class="newsletter-content">
                <h2>Subscribe for free to receive a monthly book recommendation</h2>
                <div class="newsletter-form">
                    {{! Ghost members signup form }}
                    <form data-members-form="subscribe">
                        <div class="form-group">
                            <input type="email" name="email" placeholder="Your email address" required />
                            <button type="submit">Subscribe</button>
                        </div>
                        <div class="message-success">
                            <strong>Great!</strong> Check your inbox and confirm your subscription.
                        </div>
                        <div class="message-error">
                            Please enter a valid email address.
                        </div>
                    </form>
                </div>
            </div>
        </section>
        
        {{! Latest in non-fiction}}
        <section class="section-nonfiction">
            <div class="section-header">
                <h2 class="section-heading">Latest in Non-fiction</h2>
                <a href="/tag/non-fiction/" class="section-see-all">See All</a>
            </div>
            
            {{#get "posts" filter="tag:non-fiction" limit="4" include="tags"}}
                <div class="posts-grid">
                    {{#foreach posts}}
                        <article class="post-item">
                            {{#if feature_image}}
                                <div class="post-image">
                                    <a href="{{url}}">
                                        <img src="{{img_url feature_image size="m"}}" alt="{{title}}" />
                                    </a>
                                </div>
                            {{/if}}
                            
                            <div class="post-content">
                                <h3 class="post-title">
                                    <a href="{{url}}">{{title}}</a>
                                </h3>
                                {{#if primary_tag}}
                                    <h4 class="article-author">by {{primary_tag.name}}</h4>
                                {{/if}}
                                {{#if excerpt}}
                                    <p class="post-excerpt">{{excerpt}}</p>
                                {{/if}}
                            </div>
                        </article>
                    {{/foreach}}
                </div>
            {{/get}}
        </section>
        
    </div>
</main>
